#include<stdio.h>
int main(){
	int n;
	printf("nhap mot so bat ki ");
	scanf("%d",&n);
	if(n>0)
    printf("%d la so duong",n);
	else if(n==0)
	printf("%d khong la so duong hay am",n);
	else printf("%d la so am",n);
   

return 0;
} 
